name: Update Stock Tracker

on:
  schedule:
    # Run at specific times: :00, :05, :10, :15, :20, :25, :30, :35, :40, :45, :50, :55
    - cron: '0,5,10,15,20,25,30,35,40,45,50,55 * * * *'
  workflow_dispatch:  # Allow manual trigger
  push:
    branches:
      - main
    paths:
      - 'update_stock.py'
      - '.github/workflows/update-stock.yml'

jobs:
  update-stock:
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Run stock update script
        run: python update_stock.py

      - name: Check for changes
        id: git-check
        run: |
          git diff --exit-code stock.html || echo "changed=true" >> $GITHUB_OUTPUT

      - name: Commit and push if changed
        if: steps.git-check.outputs.changed == 'true'
        run: |
          git config --local user.email "github-actions[bot]@users.noreply.github.com"
          git config --local user.name "github-actions[bot]"
          git add stock.html
          git commit -m "🔄 Auto-update stock tracker [skip ci]

          Updated at: $(date -u '+%Y-%m-%d %H:%M:%S UTC')
          Source: George's Shop API"
          git push